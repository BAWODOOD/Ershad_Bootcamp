/*
    AUTHOR: BANDAR KHALED SALEH BAWODOOD 
    DATE: 30/6/2026
    TITLE: Find Maximum of 10
*/

#include <iostream>

using namespace std;

int main(void)
{
    int num1 = 0, num2 = 0;
    cin >> num1;

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cin >> num2;
    if (num1 < num2) num1 = num2; 

    cout << "Maximum: " << num1 << endl;

    return 0;
}